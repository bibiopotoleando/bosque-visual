.card img {
  width: 90px;
  height: 90px;
  object-fit: contain;
}

.card.locked img {
  filter: grayscale(100%);
  opacity: 0.25;
}

.collection {
  background: rgba(255,255,255,.75);
  border-radius: 24px;
}
